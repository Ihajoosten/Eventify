using Eventify.Application.Interfaces.Commands;
using Eventify.Application.Interfaces.Commands.User;
using Eventify.Application.Interfaces.Dto;
using Eventify.Application.Interfaces.Handlers;

namespace Eventify.Application.Handlers.Commands
{
    /*public class UserCommandHandler : ICommandHandler<IUserDto, IUserCommand>
    {
        public IUserDto? Handle(IUserCommand command)
        {
            if (command is ICreateUserCommand createUserCommand)
            {
                return null;
            }
            else if (command is IUpdateUserCommand updateUserCommand)
            {
                return null;
            }
            else if (command is IDeleteUserCommand deleteUserCommand)
            {
                return null;
            }
            else if (command is IChangePasswordCommand changePasswordCommand)
            {
                return null;
            }
            else if (command is IChangeUserRoleCommand changeUserRoleCommand)
            {
                return null;
            }
            return null;
        }
    }*/
}
